/**
 * Action abstractions for CRUD panels.
 *
 * <p>{@link de.javaholic.toolkit.ui.crud.CrudPanel} orchestrates grid, optional editor form,
 * and actions defined in this package. Presets configure only default create/edit/delete
 * actions, while custom actions extend behavior independently. Scopes split actions into
 * toolbar, row, and selection context.</p>
 */
package de.javaholic.toolkit.ui.crud.action;
